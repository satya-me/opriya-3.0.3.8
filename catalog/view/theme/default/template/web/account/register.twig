<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous" />

    <title>Register form</title>
  </head>
  <style>
    .form_wrapper {
      max-width: 500px;
      margin: 0 auto;
      padding: 2rem 0;
    }
    
    .form_area {
      box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
      padding: 2rem;
      margin-top: 2rem;
    }
    
    .form_title h2 {
      font-size-adjust: 1.5rem;
      color: #000;
      text-transform: uppercase;
      font-weight: normal;
    }
    
    .form-group label {
      font-weight: bold;
      font-size: 1rem;
    }
    
    .required {
      color: red;
      margin-left: 0.2rem;
    }
    
    .already_account {
      margin: 1rem 0;
    }
    
    .btn_submit {
      background: #2d3436;
      color: #fff;
      border-radius: 0;
      display: inline-block;
      padding: 0.2rem 1.5rem;
      border: none;
      outline: none;
    }
    
    .form-check {
      margin-bottom: 1rem;
    }
    
    .privacy {
      font-weight: bold;
      color: red;
    }
    
    .already_account {
      text-align: center;
    }
    
    .already_account h6 {
      font-weight: normal;
    }
  </style>
  <body>
    {# resgistre form #}
    <div class="form_wrapper">
      <div class="form_inner">
        <div class="company_logo text-center">
          <a href=""><img src="{{ assets }}images/logos/logo-index3.png" alt="" /></a>
        </div>
        <div class="form_area">
          <div class="form_title">
            <h2 class="text-center">Register</h2>
          </div>
          <form action="{{ action }}" method="post" enctype="multipart/form-data" class="login">
            <div class="form-group">
              <label for="input-firstname">{{ entry_firstname }}<span class="required">*</span></label>
              <input class="form-control" type="text" name="firstname" value="{{ firstname }}" placeholder="{{ entry_firstname }}" id="input-firstname" required />
              {% if error_firstname %}
                <div class="text-danger">{{ error_firstname }}</div>
              {% endif %}
            </div>
            <div class="form-group">
              <label for="input-lastname">{{ entry_lastname }}<span class="required">*</span></label>
              <input type="text" class="form-control" name="lastname" value="{{ lastname }}" placeholder="{{ entry_lastname }}" id="input-lastname" required />
              {% if error_lastname %}
                <div class="text-danger">{{ error_lastname }}</div>
              {% endif %}
            </div>
            <div class="form-group">
              <label for="input-email">{{ entry_email }}<span class="required">*</span></label>
              <input type="email" class="form-control" name="email" value="{{ email }}" placeholder="{{ entry_email }}" id="input-email" required />
              {% if error_email %}
                <div class="text-danger">{{ error_email }}</div>
              {% endif %}
            </div>
            <div class="form-group">
              <label for="input-telephone">{{ entry_telephone }}<span class="required">*</span></label>
              <input type="tel" class="form-control" name="telephone" value="{{ telephone }}" placeholder="{{ entry_telephone }}" id="input-telephone" required />
              {% if error_telephone %}
                <div class="text-danger">{{ error_telephone }}</div>
              {% endif %}
            </div>
            <div class="form-group">
              <label for="input-password">{{ entry_password }}<span class="required">*</span></label>
              <input type="password" class="form-control" name="password" value="{{ password }}" placeholder="{{ entry_password }}" id="input-password" required />
              {% if error_password %}
                <div class="text-danger">{{ error_password }}</div>
              {% endif %}
            </div>
            <div class="form-group">
              <label for="input-confirm">{{ entry_confirm }}<span class="required">*</span></label>
              <input type="password" class="form-control" name="confirm" value="{{ confirm }}" placeholder="{{ entry_confirm }}" id="input-confirm" required />
              {% if error_confirm %}
                <div class="text-danger">{{ error_confirm }}</div>
              {% endif %}
            </div>
            {{ captcha }}
            {% if text_agree %}
              <div class="buttons">
                <div class="pull-right">
                  {{ text_agree }}
                  {% if agree %}
                    <input type="checkbox" name="agree" value="1" checked="checked" />
                  {% else %}
                    <input type="checkbox" name="agree" value="1" />
                  {% endif %}&nbsp;<input type="submit" value="{{ button_continue }}" class="btn btn-primary" />
                </div>
              </div>
            {% else %}
              <div class="buttons">
                <div class="pull-right">
                  <input type="submit" value="{{ button_continue }}" class="btn btn-primary" />
                </div>
              </div>
            {% endif %}
            {# <div class="form-check">
              <input name="rememberme" type="checkbox" id="rememberme" value="forever" class="form-check-input" />
              <label class="form-check-label" for="rememberme">{{ text_agree }}</label>
            </div>
            <input type="submit" class="button" value="{{ button_continue }}" /> #}
          </form>
          <div class="already_account">
            <h6>I Have Account? <a href="http://" class="login_style">Login</a></h6>
          </div>
        </div>
      </div>
    </div>

    <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js" integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
  </body>
</html>
